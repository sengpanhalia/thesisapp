package com.example.thesisapp

import io.flutter.embedding.android.FlutterActivity

// The "savePdfToDownloads" channel that lived here served the receipt's Save
// PDF button. The receipt is saved to Photos only now (image_gallery_saver),
// so the channel had no caller and is gone.
class MainActivity : FlutterActivity()
